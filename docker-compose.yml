version: "3.4"

services:
  service:
    build: .
    container_name: service
  mongo:
    container_name: mongo
    image: mongo:latest
    ports:
      - 27100:27017
    volumes:
      - ./backup/backup
